---
description: 복지 공고를 검색해 리포트로 정리하고 카카오톡 나챗방으로 보냅니다
argument-hint: [검색 조건 — 예: 30세 서울 청년 주거]
---

`bokji` 스킬을 실행한다.

검색 조건: $ARGUMENTS

조건이 비어 있으면 `~/.claude/bokji-profile.json` 에 저장된 프로필로 실행한다.
프로필도 없으면 스킬 절차대로 프로필을 먼저 수집한다.
